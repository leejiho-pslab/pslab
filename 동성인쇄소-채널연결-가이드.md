# 동성인쇄소 채널 연결 가이드 (계정 연결 전용)

자동 발행을 위해 **인스타그램·스레드**는 토큰을 발급해 연결하고,
**네이버 블로그**는 연결 없이 대시보드에서 복붙 발행합니다.

발급한 값은 모두 아래 한 곳에 넣습니다 (토큰은 **코드에 절대 넣지 마세요**).

🔗 **시크릿 등록:** https://github.com/leejiho-pslab/pslab/settings/secrets/actions
→ **New repository secret** → 이름/값 붙여넣기 → **Add secret**

> 💡 어느 단계든 막히면 "인스타 연결 3번 막혔어"처럼 말씀하세요. 화면 보며 짚어드립니다.
> 각 화면을 캡처해 보내주시면 정확히 어디를 누를지 알려드릴 수 있습니다.

---

## 채널 1 — 인스타그램 연결

### 준비물 (먼저 이 3가지)
1. **인스타그램을 프로페셔널(비즈니스) 계정으로 전환**
   - 인스타 앱 → 프로필 → ☰ → 설정 → **계정 유형 및 도구** → **프로페셔널 계정으로 전환** → 비즈니스
2. **페이스북 페이지와 연결** (없으면 하나 새로 생성)
   - 인스타 앱 → 프로필 편집 → **페이지 연결**, 또는 페이스북 페이지 설정에서 인스타 계정 연결
3. **Meta 개발자 앱** 준비
   - https://developers.facebook.com/apps → **앱 만들기** → 유형 **비즈니스** → 이름 입력해 생성

### 발급 단계
4. **Graph API 탐색기** 열기: https://developers.facebook.com/tools/explorer
   - 우측 상단에서 방금 만든 앱 선택
   - **Add permissions**(권한)에서 아래 5개 체크 후 **Generate Access Token**:
     `instagram_basic`, `instagram_content_publish`, `pages_show_list`,
     `pages_read_engagement`, `business_management`
   - 팝업에서 동성인쇄소 페이지/인스타 계정 **모두 허용**
5. **인스타 계정 ID 찾기** (탐색기 주소창에 하나씩 입력해 실행 ▶)
   - `me/accounts` → 결과에서 동성인쇄소 페이지의 `id` 복사
   - `{위에서복사한페이지ID}?fields=instagram_business_account` 실행
     → 나오는 `instagram_business_account.id` 가 바로 **IG User ID** 입니다.
6. **장기 토큰(60일)으로 교환** — 탐색기 토큰은 짧아서, 아래를 주소창에 넣어 실행:
   ```
   https://graph.facebook.com/v21.0/oauth/access_token?grant_type=fb_exchange_token&client_id=<앱ID>&client_secret=<앱시크릿>&fb_exchange_token=<5번탐색기토큰>
   ```
   (앱ID·앱시크릿 = 개발자 앱 → 설정 → 기본 설정) → 나오는 `access_token` 이 **장기 토큰**.

### 넣을 시크릿 (2개)
| 이름 (그대로 복사) | 값 |
|---|---|
| `PSLAB_INSTAGRAM_ACCESS_TOKEN` | 6번의 장기 토큰 |
| `PSLAB_INSTAGRAM_IG_USER_ID` | 5번의 IG User ID |

> ⚠️ 토큰은 **60일 만료**입니다. 대시보드에 🔑 경고가 뜨면 4~6번을 다시 해 값만 갱신하세요.
> 게시물이 일부공개로 보이면, 인스타 계정이 **공개(프로페셔널)** 인지 확인하세요.

---

## 채널 2 — 스레드 연결

스레드는 인스타와 별개의 API입니다(같은 Meta 앱에서 진행 가능).

### 준비물
1. **스레드 계정**(동성인쇄소 인스타와 연동된 계정)
2. Meta 개발자 앱에 **Threads API 사용 사례(Use case)** 추가
   - https://developers.facebook.com/apps → 앱 선택 → **제품 추가/사용 사례** 에서 **Threads** 추가

### 발급 단계
3. **권한 동의(OAuth)** 로 액세스 토큰 발급 — 스코프 2개:
   `threads_basic`, `threads_content_publish`
   - 앱의 Threads 설정에서 토큰 생성(또는 OAuth 리디렉션)으로 동성인쇄소 스레드 **허용**
4. **스레드 User ID 찾기** — 아래를 주소창에 넣어 실행:
   ```
   https://graph.threads.net/v1.0/me?fields=id,username&access_token=<3번토큰>
   ```
   → 나오는 `id` 가 **Threads User ID** 입니다.
5. **장기 토큰(60일)으로 교환**:
   ```
   https://graph.threads.net/v1.0/access_token?grant_type=th_exchange_token&client_secret=<앱시크릿>&access_token=<3번토큰>
   ```
   → 나오는 `access_token` 이 장기 토큰.

### 넣을 시크릿 (2개 필수 + 2개 선택)
| 이름 | 값 |
|---|---|
| `PSLAB_THREADS_ACCESS_TOKEN` | 5번의 장기 토큰 |
| `PSLAB_THREADS_THREADS_USER_ID` | 4번의 Threads User ID |
| `PSLAB_THREADS_APP_ID` *(선택)* | 앱 ID — 넣으면 만료일 계산·경고 |
| `PSLAB_THREADS_APP_SECRET` *(선택)* | 앱 시크릿 — 위와 같은 용도 |

> ⚠️ 스레드 토큰도 **60일 만료** → 대시보드 🔑 경고 시 3~5번 재발급.

---

## 채널 3 — 네이버 블로그 (연결 불필요)

네이버는 **공식 발행 API가 없어** 토큰 연결을 하지 않습니다. 대신 자동화가 글·이미지를
다 만들어 두고, 대시보드에서 **복사 + 이미지 다운로드**로 담당자가 직접 게시합니다.

- 시크릿 등록: **없음**
- 발행 방법: 대시보드 → **네이버 블로그 탭** → 완성된 본문 **복사**, 대표/본문 이미지 **다운로드**
  → 네이버 블로그 글쓰기에 붙여넣고 게시

---

## 연결 확인 (다 넣은 뒤)

1. 🔗 https://github.com/leejiho-pslab/pslab/actions → **pslab-cron** → **Run workflow** 실행
2. 잠시 후 대시보드( https://leejiho-pslab.github.io/pslab/ )에서:
   - 연결한 채널이 **"정상"**(🔑 경고 없음)으로 뜨는지
   - 네이버 탭에 복붙 도우미가 보이는지
   확인하면 계정 연결 완료입니다.

> 여기까지가 "③ 채널 계정 연결" 단계입니다. 그다음은 ④ 채널별 운영 전략 정의 →
> ⑤ 기획 검수 → ⑥ 콘텐츠 생성 → ⑦ 자동화 ON·텔레그램 순서로 진행합니다.

---

### 참고 — 나중에 다른 채널도 붙일 때 (구글블로그·유튜브·링크드인)

동성인쇄소는 지금 3채널이지만, 나중에 확장하면 아래 시크릿이 필요합니다(스킬 §4 참고).
- 구글 블로그: `PSLAB_BLOGGER_CLIENT_ID` / `CLIENT_SECRET` / `REFRESH_TOKEN` / `BLOG_ID`
- 유튜브: `PSLAB_YOUTUBE_CLIENT_ID` / `CLIENT_SECRET` / `REFRESH_TOKEN` / `CHANNEL_ID`
- 링크드인: `PSLAB_LINKEDIN_ACCESS_TOKEN` / `PSLAB_LINKEDIN_AUTHOR_URN`
