# pslab 오픈 가이드 (대표님 체크리스트)

내일 오픈까지 **순서대로** 따라 하시면 됩니다. 코드/시스템은 전부 준비됐고,
남은 건 **연결(시크릿/설정)** 과 **수동 채널 발행**뿐입니다.

대시보드 상단 **"🚀 오픈 준비 체크리스트"** 패널에서 진행 상황을 실시간으로 볼 수 있습니다.
👉 https://leejiho-pslab.github.io/pslab/

---

## 1단계 — 필수: 발행 토큰 확인 (인스타·스레드)
이미 연결돼 실제 발행까지 성공한 상태입니다. **토큰은 60일이면 만료**되므로,
대시보드에 🔑 토큰 경고가 뜨면 새로 발급해 아래 시크릿을 갱신하세요.

- 시크릿 위치: 저장소 **Settings → Secrets and variables → Actions**
  - `PSLAB_INSTAGRAM_ACCESS_TOKEN`, `PSLAB_INSTAGRAM_IG_USER_ID`
  - `PSLAB_THREADS_ACCESS_TOKEN`, `PSLAB_THREADS_THREADS_USER_ID`

## 2단계 — 텔레그램 푸시 켜기 (발행 알림 + 주간 리포트)
1. 텔레그램에서 **@BotFather** 검색 → `/newbot` → 봇 이름 정하면 **봇 토큰** 발급
2. 만든 봇과 대화창에서 아무 메시지나 한 번 보냄
3. 브라우저에서 `https://api.telegram.org/bot<봇토큰>/getUpdates` 열어 `chat.id` 확인
4. 저장소 시크릿에 추가: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`
→ 이후 발행될 때마다, 매주 월요일 주간 리포트가 텔레그램으로 옵니다.

## 3단계 — AI 글·코멘트 켜기 (선택, 품질 ↑)
- `ANTHROPIC_API_KEY` 시크릿 추가 → 글 생성·인사이트 코멘트·주간 리포트를 대표님 톤으로 AI가 작성
- 없어도 규칙 기반으로 동작합니다(품질만 차이).

## 4단계 — 기획안 검수 (대시보드)
- 대시보드 **"📅 7월 콘텐츠 기획안"** 에서 채널별 30건을 확인
- 카드를 클릭하면 전체 캡션·캐러셀이 보입니다
- 고칠 게 있으면 **"✏️ 수정요청"** 버튼(깃허브 이슈로 전달) 또는 저에게 말씀하세요

## 5단계 — 실제 자동발행 ON (준비되면)
- 저장소 **Settings → Variables → Actions** 에 `PSLAB_DRY_RUN` = `false` 추가
- 그 순간부터 cron(하루 2회, 한국시간 11시·19시)이 **예정 시각이 지난 기획안을
  인스타·스레드에 자동 발행**합니다. (그 전까진 안전하게 시뮬레이션만)

## 6단계 — 수동 채널 발행 (네이버 블로그·유튜브)
- 이 둘은 자동발행 API가 없어 **복붙 발행**입니다.
- 대시보드 해당 채널 탭에서 완성된 글/대본을 복사해 직접 게시하세요.

---

## 평소 운영 (오픈 후)
1. **하루 2회 cron** 이 자동으로: 토큰 점검 → 예약 발행 → 성과 수집 → 인사이트
   코멘트 → 대시보드 갱신
2. **매주 월요일** 지난주 종합 리포트가 생성되고 텔레그램으로 옵니다
3. **자체 학습**: 발행이 쌓일수록 어떤 디자인(A/B/C)·시간대·소재가 잘 먹히는지
   학습해 다음 기획에 자동 반영됩니다
4. 토큰 만료 경고가 뜨면 1단계처럼 갱신만 해주시면 됩니다

## 문제가 생기면
- 대시보드가 안 바뀜 → 강력 새로고침(Ctrl/Cmd+Shift+R). 그래도 안 되면 GitHub
  **Settings → Pages → Source = "GitHub Actions"** 인지 확인
- 발행 실패 → 대부분 토큰 만료. 대시보드 🔑 경고 확인 후 1단계 갱신
