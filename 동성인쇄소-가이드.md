# 동성인쇄소 SNS 자동운영 — 따라 하기 가이드

**링크를 열고, 복사해서 붙여넣기만** 하면 끝나도록 만들었습니다.
코드/설정/콘텐츠는 이미 제가 다 준비해 뒀고, 남은 건 **연결(토큰·설정)** 뿐입니다.

- 채널: **인스타그램 + 스레드 + 구글 블로그 + 네이버 블로그**
- 발행: **매일 저녁 6시(18:00)** 자동, 채널을 번갈아
- 대시보드(모든 결과 확인): 👉 https://leejiho-pslab.github.io/pslab/

> ✅ = 제가 이미 완료 / ⬜ = 대표님이 하실 일 (아래 순서대로)

---

## ✅ (완료) 시스템 준비 — 제가 해둔 것

- ✅ 동성인쇄소 설정 파일 생성 (`clients/dongsung.json`)
- ✅ 브랜드 디자인 팔레트 (딥네이비+화이트+레드, 신뢰감 있는 톤)
- ✅ **초기 콘텐츠 12건** 기획·작성 완료 (7/15 ~ 7/26, 매일 18시)
  - 인스타 카드뉴스 4건: 명함 수량별 단가 / 전단지 종이 3종 / 후가공(코팅·박·형압) / 현수막 실외용
  - 스레드 팁 4건: 급행 주문법 / 도무송 쉽게 / 데이터 실수 3가지 / 소량인쇄
  - 네이버 블로그 4건: 명함 A to Z / 전단지 홍보 팁 / 현수막 체크리스트 / 스티커 활용법

여기까지는 **손대실 것 없습니다.** 아래 ⬜ 만 순서대로 하시면 됩니다.

---

## ⬜ 1단계 — 인스타·스레드 연결 (자동 발행용 열쇠)

동성인쇄소 인스타그램을 **전문가(비즈니스) 계정**으로 바꾸고, Meta에서 발행용 토큰을 받습니다.
(이 과정은 화면을 보며 **어디를 누를지 하나씩 짚어드릴 수 있습니다** — "인스타 연결 도와줘"라고 하세요.)

받은 값 4개를 아래 링크에 넣습니다.

🔗 **시크릿 등록:** https://github.com/leejiho-pslab/pslab/settings/secrets/actions
→ 초록색 **New repository secret** 버튼 → 이름/값 붙여넣기 → Add secret (4번 반복)

| 시크릿 이름 (그대로 복사) | 넣을 값 |
|---|---|
| `PSLAB_INSTAGRAM_ACCESS_TOKEN` | 인스타 장기 액세스 토큰 |
| `PSLAB_INSTAGRAM_IG_USER_ID` | 인스타 비즈니스 계정 ID |
| `PSLAB_THREADS_ACCESS_TOKEN` | 스레드 액세스 토큰 |
| `PSLAB_THREADS_THREADS_USER_ID` | 스레드 사용자 ID |

**구글 블로그(Blogger)도 자동 발행 채널입니다.** 토큰 4개(`PSLAB_BLOGGER_CLIENT_ID`/`CLIENT_SECRET`/
`REFRESH_TOKEN`/`BLOG_ID`)를 같은 시크릿 페이지에 넣습니다. 발급 방법은 **`동성인쇄소-채널연결-가이드.md`
채널 3(구글 블로그)** 또는 상세본 **`구글블로그-연동.md`** 를 따라 하세요.

> 💡 인스타 계정 핸들이 `dongsung_print`가 아니면 알려주세요 — 설정의 계정 주소만 바꿔드립니다.
> 채널별 계정 연결 전체 안내는 **`동성인쇄소-채널연결-가이드.md`** 한 파일에 정리돼 있습니다.

---

## ⬜ 2단계 — 텔레그램 알림 켜기 (발행/실패 즉시 통보)

발행될 때마다, 문제가 생기면 **텔레그램으로 바로** 알려줍니다.

1. 텔레그램에서 **@BotFather** 검색 → `/newbot` → 봇 이름 정하면 **봇 토큰**이 나옵니다.
2. 방금 만든 봇과의 대화창에 아무 메시지나 한 번 보냅니다.
3. 브라우저 주소창에 아래를 붙여넣고 `<봇토큰>`만 바꿔 열면 `chat.id` 숫자가 보입니다.
   ```
   https://api.telegram.org/bot<봇토큰>/getUpdates
   ```
4. 🔗 https://github.com/leejiho-pslab/pslab/settings/secrets/actions 에서 2개 등록:

| 시크릿 이름 | 넣을 값 |
|---|---|
| `TELEGRAM_BOT_TOKEN` | 1번의 봇 토큰 |
| `TELEGRAM_CHAT_ID` | 3번의 chat.id 숫자 |

---

## ⬜ 3단계 — AI 글쓰기 켜기 (선택, 품질 ↑)

넣으면 글을 동성인쇄소 톤으로 AI가 더 다듬어 씁니다. **없어도** 준비된 12건은 그대로 발행됩니다.

🔗 https://github.com/leejiho-pslab/pslab/settings/secrets/actions

| 시크릿 이름 | 넣을 값 |
|---|---|
| `ANTHROPIC_API_KEY` | 클로드 API 키 |

그리고 비용 절감을 위해 🔗 https://github.com/leejiho-pslab/pslab/settings/variables/actions 에서
**New repository variable** → 이름 `PSLAB_CLAUDE_MODEL`, 값 `claude-haiku-4-5` 등록.

---

## ⬜ 4단계 — 대시보드 켜기 (결과 확인 화면)

🔗 https://github.com/leejiho-pslab/pslab/settings/pages
→ **Source** 를 **GitHub Actions** 로 선택 → 저장.

> ⚠️ 저장소는 **공개(public)** 상태를 유지하세요. 비공개면 무료 플랜에서 대시보드가 안 뜹니다(404).
> 시크릿(토큰)은 공개 저장소여도 **절대 노출되지 않습니다.**

잠시 후 여기서 확인: 👉 https://leejiho-pslab.github.io/pslab/

---

## ⬜ 5단계 — 미리보기로 확인 (아직 진짜 발행 아님)

지금은 **안전 모드(시뮬레이션)** 라 실제로 올라가지 않습니다. 먼저 화면부터 확인하세요.

🔗 실행: https://github.com/leejiho-pslab/pslab/actions
→ 왼쪽에서 **pslab-cron** 워크플로 클릭 → 오른쪽 **Run workflow** 버튼 → 실행.

→ 대시보드( https://leejiho-pslab.github.io/pslab/ )에서:
- 예정된 12건 콘텐츠 카드
- 인스타 이미지·네이버 블로그 본문 미리보기
- 오픈 체크리스트

가 보이면 정상입니다. 고칠 내용은 대시보드 **✏️ 수정요청** 버튼(또는 저에게 말씀).

---

## ⬜ 6단계 — 진짜 발행 ON (준비되면 딱 한 줄)

🔗 https://github.com/leejiho-pslab/pslab/settings/variables/actions
→ **New repository variable** → 이름 `PSLAB_DRY_RUN`, 값 `false` 등록.

이 순간부터 **매일 저녁 6시**, 예정된 콘텐츠가 인스타·스레드에 **자동 발행**되고
텔레그램으로 알림이 옵니다. (그 전까지는 계속 안전 시뮬레이션)

> 네이버 블로그는 자동발행 API가 없어, 대시보드 **네이버 탭**에서 완성된 글을
> **복사 + 이미지 다운로드**해 직접 올리시면 됩니다(하루 1건씩 안내).

---

## 평소엔 이렇게만 (오픈 후)

1. **매일 저녁 6시** 자동 발행 → 텔레그램 알림 확인
2. **대시보드**에서 성과 보기 / 다음 콘텐츠 검수 / 수정요청
3. **🧭 지침 탭**에 "이번 달은 현수막 위주로", "명함 후가공 강조" 같이 적으면 다음 콘텐츠에 자동 반영
4. **네이버 블로그**만 대시보드에서 복붙 발행
5. 🔑 토큰 만료 경고가 뜨면 → 1단계처럼 값만 새로 갱신

---

## 막히면

- **대시보드가 안 바뀜** → 강력 새로고침(Ctrl/⌘+Shift+R). 그래도면 4단계 Pages 설정 확인
- **발행이 안 됨** → 대부분 토큰 만료. 대시보드 🔑 경고 확인 후 1단계 값 갱신
- **어느 단계든 막히면** → 저에게 "○단계 막혔어"라고 하시면 화면 보며 짚어드립니다

---

### 참고 — 콘텐츠·설정을 더 바꾸고 싶다면 (개발 메모)

- 설정: `clients/dongsung.json` (계정 핸들·발행시각·톤)
- 콘텐츠 재생성: `node scripts/seed-dongsung.mjs` → `node scripts/schedule-daily.mjs --client dongsung --start <날짜>`
- 데이터 폴더(`data/clients/dongsung/`)는 gitignore 대상이라 시드 파일은 **`git add -f`** 로 커밋해야 CI가 렌더합니다.
