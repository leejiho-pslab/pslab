# 동성인쇄소 SNS 자동화 — 기본 가이드

동성인쇄소의 SNS를 **무료로 24시간 무인 운영**하기 위한 시작 가이드입니다.
이 저장소(`pslab`)의 자동화 시스템을 그대로 재사용해, 동성인쇄소를 새 업체(client)로
추가하는 방식으로 세팅합니다.

> 핵심 철학: **대시보드가 곧 제품이다.** 모든 결과물(글·이미지·영상·성과)은
> 대시보드(GitHub Pages) 링크에서 확인할 수 있습니다. 담당자는 코딩 없이
> 대시보드만 보면 됩니다.

---

## 0. 무엇을 하게 되나 (한눈에)

1. 매일 정해진 시각(기본 **18:00**)에 인쇄소 콘텐츠가 **자동 발행**됩니다.
2. 글은 AI(클로드)가 동성인쇄소 톤으로 쓰고, 한글 이미지·카드·쇼츠 영상까지 자동 제작.
3. 발행 → 반응 수집 → 학습 → 다음 기획 반영의 **자체 학습 선순환**이 돕니다.
4. 담당자는 **대시보드**에서 예정 콘텐츠 검수·수정요청·성과 확인만 하면 됩니다.

---

## 1. 브랜드 정보 (먼저 채울 것)

아래 값을 확정하면 설정 파일을 만들 수 있습니다. *괄호 안은 인쇄소 기준 예시*입니다.

| 항목 | 내용 | 예시 |
|---|---|---|
| 업체 ID | 영문 소문자 식별자 | `dongsung` |
| 업체명 | 표시용 이름 | 동성인쇄소 |
| 업종 | | 종합 인쇄 / 명함·전단·현수막·스티커 등 |
| 핵심 주제(keywords) | 콘텐츠 소재 풀 | 명함인쇄, 전단지, 현수막, 스티커, 소량인쇄, 급행인쇄, 후가공(박·형압·코팅) |
| 브랜드 톤(brandTone) | 말투 | 신뢰감 있고 친절하게, 전문 용어는 쉽게 풀어서 |
| 타깃(audience) | 누구에게 | 소상공인·자영업자, 창업자, 소규모 사업자, 디자이너, 이벤트 준비하는 개인 |
| 콘텐츠 기둥(contentPillars) | 반복 테마 | ①인쇄물 종류·활용 팁 ②종이·후가공 지식 ③주문 노하우(입고~납기) ④실제 제작 사례 ⑤가격·소량/급행 안내 |
| 발행 시각 | 하루 몇 시 | 18:00 (필요시 2회) |
| 금지어(bannedWords) | 쓰면 안 되는 말 | 최저가, 100% 보장, 무조건 |
| 채널 | 운영할 SNS | §3 참고 |

> **인쇄소 콘텐츠 아이디어 예시** — "명함 300장 vs 500장, 실제 단가 차이", "전단지 종이 종류
> 3가지와 느낌 비교", "현수막 실사출력, 실외용은 뭐가 다를까", "급하게 명함 필요할 때 급행
> 주문하는 법", "박·형압·에폭시 — 후가공 한 장으로 보는 차이", "스티커 재단(도무송) 쉽게
> 이해하기". 이런 소재가 `keywords`·`contentPillars`에 녹아들면 AI가 알아서 변주합니다.

---

## 2. 설정 파일 만들기 (개발/세팅 담당)

기존 클라이언트(`clients/pslab.json`)를 템플릿으로 복사해 값만 교체합니다.

### 2-1. `clients/dongsung.json`

```json
{
  "id": "dongsung",
  "name": "동성인쇄소",
  "industry": "종합 인쇄 (명함·전단·현수막·스티커·소량/급행)",
  "keywords": ["명함인쇄","전단지","현수막","스티커","소량인쇄","급행인쇄","인쇄후가공","종이추천","카탈로그","봉투인쇄"],
  "brandTone": "신뢰감 있고 친절하게 — 전문 용어는 쉽게 풀어서, 과장 없이",
  "audience": "소상공인·자영업자, 창업자, 소규모 사업자, 디자이너, 행사 준비하는 개인",
  "contentPillars": ["인쇄물 종류·활용 팁","종이·후가공 지식","주문 노하우(입고~납기)","제작 사례","가격·소량/급행 안내"],
  "bannedWords": ["최저가","100% 보장","무조건"],
  "targets": ["instagram", "threads", "naver-blog"],
  "accounts": { "instagram": "dongsung_print", "threads": "dongsung_print" },
  "channelLinks": {
    "instagram": "https://www.instagram.com/dongsung_print",
    "threads": "https://www.threads.net/@dongsung_print"
  },
  "scheduleTimes": ["18:00"],
  "reviewMode": "manual",
  "reportTo": "담당자이메일@example.com"
}
```

> `accounts`·`channelLinks`의 핸들/URL은 **실제 동성인쇄소 계정**으로 교체하세요.
> `reviewMode`는 누락하면 오류가 나니 반드시 넣습니다.

### 2-2. `data/clients/dongsung/design.json`

브랜드 이미지 팔레트입니다. 인쇄소는 **신뢰·정밀·깔끔함**이 어울립니다.

```json
{
  "palette": "딥네이비 + 화이트 + 포인트 레드, 고대비·깔끔한",
  "mood": "신뢰감 있고 정확하며 전문적인",
  "composition": "좌측 텍스트 + 우측 여백의 비대칭 구도",
  "motifs": ["종이/인쇄 모티프", "굵은 산세리프 헤드라인", "여백"],
  "avoid": ["과한 스톡사진 느낌", "잡다한 요소", "저화질", "클립아트"]
}
```

> ⚠️ **함정**: `data/clients/**`는 기본 gitignore 대상입니다. 시드/설정 파일은 반드시
> `git add -f data/clients/dongsung/design.json` 처럼 **강제 커밋**해야 CI가 렌더를
> 건너뛰지 않습니다.

---

## 3. 채널 선택 & 자동화 가능 여부

인쇄소는 **지역 기반 B2B/B2C**라 인스타그램·네이버 블로그가 특히 효과적입니다.

| 채널 | 자동발행 | 인쇄소 적합도 | 비고 |
|---|---|---|---|
| 인스타그램 | ✅ | ★★★ | 제작 사례·비포애프터 캐러셀에 최적 |
| 스레드 | ✅ | ★★ | 짧은 인쇄 팁·소통 |
| 네이버 블로그 | ❌ 수동 | ★★★ | 지역 검색 유입 최강 — 대시보드에서 복붙 발행 |
| 구글 블로그 | ✅ | ★★ | 완전 자동, 검색 노출 |
| 유튜브(쇼츠) | ✅ | ★★ | 제작 과정 15초 모션 |
| 링크드인 | ✅ | ★ | B2B(대량 발주처) 대상이면 |

> **추천 시작 조합**: `인스타그램 + 스레드 + 네이버 블로그`.
> 자동 3채널이 부담되면 인스타 하나로 시작해 늘려도 됩니다.

---

## 4. 채널 연동 (담당자가 발급 → GitHub Secrets에 등록)

토큰은 **코드에 절대 넣지 말고** 저장소 **Settings → Secrets and variables → Actions**에 넣습니다.

- **인스타그램/스레드**: Meta 개발자 앱 → 인쇄소 IG 비즈니스(전문가) 계정 연결 → 장기 토큰
  - `PSLAB_INSTAGRAM_ACCESS_TOKEN`, `PSLAB_INSTAGRAM_IG_USER_ID`
  - `PSLAB_THREADS_ACCESS_TOKEN`, `PSLAB_THREADS_THREADS_USER_ID`
  - ⚠️ 게시물이 일부공개로 보이면 앱에서 계정을 **공개(전문가 계정)**로 전환
- **네이버 블로그**: 자동발행 API 없음 → 대시보드에서 완성 글 **복붙 + 이미지 다운로드**로 게시
- **구글 블로그(선택)**: Blogger API v3, OAuth `refresh_token` 발급
- **유튜브(선택)**: YouTube Data API v3, `refresh_token`. 앱 미검수면 7일마다 재발급 필요
- **텔레그램 알림**: @BotFather로 봇 생성 → `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`
- **AI 글**: `ANTHROPIC_API_KEY` (+ 비용절감 Variables `PSLAB_CLAUDE_MODEL=claude-haiku-4-5`)

> 각 발급 과정은 화면을 보며 **어디를 누를지** 짚어드릴 수 있습니다. 필요하면
> `구글블로그-연동.md`처럼 동성인쇄소용 채널별 문서를 따로 만들어 드립니다.

---

## 5. 세팅 순서 (체크리스트)

```
[ ] 1. clients/dongsung.json 작성 (accounts·channelLinks·scheduleTimes)
[ ] 2. data/clients/dongsung/design.json 작성 → git add -f 로 강제 커밋
[ ] 3. seed-plan / generate 로 초기 기획안(plan.json) 생성
[ ] 4. 채널 토큰 → GitHub Secrets 등록
[ ] 5. ANTHROPIC_API_KEY (+ PSLAB_CLAUDE_MODEL=claude-haiku-4-5) 등록 (선택)
[ ] 6. node scripts/schedule-daily.mjs --client dongsung  → 매일 18시 배치
[ ] 7. Variables PSLAB_DRY_RUN=false  (그 전엔 안전 시뮬레이션)
[ ] 8. Settings → Pages → Source = GitHub Actions  (저장소는 public 유지)
[ ] 9. 워크플로 수동 실행 → 대시보드 5/5 확인 → 첫 발행·텔레그램 알림 확인
```

- **PSLAB_DRY_RUN**은 기본 `true`(가짜 발행 없이 시뮬레이션). 준비되면 `false`로 켭니다.
- cron은 `0 9 * * *`(=한국 18:00) + `30 9 * * *`(보정). 재실행해도 **중복 발행 안 됨**(멱등).

---

## 6. 평소 운영 (오픈 후, 담당자용)

1. **하루 1회 cron**이 자동으로: 토큰 점검 → 예약 발행 → 성과 수집 → 인사이트 → 대시보드 갱신
2. **대시보드**(`https://<owner>.github.io/pslab`)에서:
   - 예정 콘텐츠 검수, **✏️ 수정요청**(깃허브 이슈로 전달), 성과·기간별 KPI 확인
   - **🧭 지침 탭**에 "이번 달은 현수막 위주로", "명함 후가공 강조" 같은 방향을 적으면
     다음 기획에 자동 반영됩니다
   - **네이버 블로그**는 해당 탭에서 글 복사 + 본문 이미지 다운로드 후 직접 게시
3. **자체 학습**: 발행이 쌓일수록 잘 먹히는 디자인·시간대·소재를 학습해 자동 반영
4. **토큰 만료 경고**(🔑)가 뜨면 해당 채널 토큰만 재발급해 시크릿 갱신

---

## 7. 비용

- **거의 전부 무료**: GitHub Actions, Pages(public repo), 이미지·영상 렌더(Chromium/ffmpeg)
- **유료는 선택만**: 클로드 API(Haiku 월 ~1~2천원), 힉스필드 쇼츠 영상(편당 ~10크레딧)
- 콘솔에서 사용 한도를 걸어두면 초과 없이 안전

---

## 8. 반드시 아는 함정 (요약)

- **한글 텍스트는 HTML→이미지 렌더**로만. AI 확산 이미지에 한글 넣으면 깨집니다.
- **저장소는 public 유지** — 무료 플랜은 private면 대시보드(Pages)가 404. 시크릿은 안전.
- **`data/clients/**` 시드 파일은 `git add -f`** — 안 하면 CI가 조용히 렌더를 건너뜁니다.
- **커밋 메시지에 `[skip ci]` 금지** — Pages 배포까지 막힙니다.
- 발행이 안 되는 것 같으면 ①다음 예정 시각이 실제로 지났는지 ②Pages/공개설정이
  바뀌지 않았는지부터 확인하세요.

---

## 다음 단계

이 가이드로 방향이 맞다면, 실제 세팅을 진행하겠습니다. 시작 전에 **딱 세 가지**만
확정해 주세요.

1. **채널** — 추천 조합(인스타+스레드+네이버 블로그)으로 갈까요, 인스타 하나로 시작할까요?
2. **발행 시각** — 18:00 하루 1회로 시작할까요?
3. **계정 핸들** — 동성인쇄소 인스타/스레드 등 실제 계정 주소

확정되면 `clients/dongsung.json`·`design.json` 생성부터 초기 기획안까지 세팅해 드립니다.
