---
name: sns채널-자동화
description: >-
  SNS 채널 자동 운영 시스템 — 브랜드/업체의 SNS(인스타그램·스레드·구글블로그·네이버블로그·유튜브·링크드인)를
  24시간 무인 자동 운영하는 시스템을 새 업체에 세팅하거나 점검/수정할 때 사용. 매일 정해진 시각 자동발행,
  AI(클로드) 글 생성, 한글 카드/블로그 이미지 렌더(대표이미지 포함), 유튜브 쇼츠 모션영상(힉스필드)+무료 BGM,
  실시간 대시보드(GitHub Pages, 채널 바로가기 포함), 성과 자체학습 선순환까지 포함. "다른 업체에도 적용",
  "새 클라이언트 SNS 자동화 세팅", "SNS 무인 운영 붙여줘", "대시보드가 안 뜬다", "발행이 멈췄다" 같은
  요청에 사용. 신규 업체는 반드시 "§0 표준 진행 순서"(①브랜드 정의·분석 → ②대시보드 뼈대만 →
  ③채널 계정 연결 → ④채널별 운영 전략 정의 → ⑤콘텐츠 기획·검수 → ⑥콘텐츠 15일치 생성 →
  ⑦자동화 ON·텔레그램 보고)를 **단계별 검수 게이트**로 순차 진행할 것.
---

# SNS 채널 자동화 (sns채널-자동화)

브랜드 하나의 SNS를 **무료로 24시간 무인 운영**하는 완성형 시스템. 이 저장소(`pslab`)가
레퍼런스 구현이며, 정해진 절차대로 **새 업체(client)를 추가**하면 그대로 재사용된다.
이 파일은 **라우터**다 — 실제 지침은 아래 `docs/NN-*.md`에 개념별로 나뉘어 있다.

> 핵심 철학: **대시보드가 곧 제품이다.** 모든 작업 결과는 대시보드(GitHub Pages)에서
> 링크로 확인 가능해야 한다. 비개발자 담당자를 위해 설명은 **쉬운 한국어**로.

> 🧠 **작업 시작 전 [`adr/README.md`](adr/README.md)(결정 기록 인덱스)와 관련 `docs/`를 먼저 확인**한다.
> 이 스킬은 **docs/(어떻게=지침) + adr/(왜=결정 이력)** 2계층으로 기억을 유지한다. 중요한 결정·
> 반복 함정을 발견하면 **ADR을 추가**하고 달라진 절차는 해당 `docs/` 문서에 반영한다.

---

## 표준 진행 순서 (기본 실행 계약 · 반드시 이 순서로)

> **신규 업체 요청("○○ SNS 자동화 세팅해줘")을 받으면 아래 7단계를 순서대로 밟는다.** 각 단계 끝에는
> **검수 게이트(🚦)**가 있어 담당자 승인 전에는 다음으로 넘어가지 않는다. **절대 앞질러 콘텐츠를 대량
> 생성하지 말 것** — 콘텐츠 생성(⑥)은 ④ 채널 전략과 ⑤ 기획 검수가 끝난 뒤에만.

1. **브랜드 정의·분석** — `clients/<id>.json` + `brand-brief.json` 초안 → 🚦 브랜드 정의 승인.
2. **대시보드 뼈대만** — `design.json` + 빈 `plan.json`, 대시보드 뜨는 것까지만 → 🚦 링크 열림.
3. **채널 계정 연결** — 토큰 발급 → GitHub Secrets, `check-tokens` 점검 → 🚦 연결됨 확인.
4. **채널별 운영 전략 정의** — 포맷·주제·빈도를 지침(`channel-guides.json`)에 반영 → 🚦 전략 승인.
5. **콘텐츠 기획→검수** — 기획안(`plan.json` status=planned) 생성·검수 → 🚦 기획 검수 통과.
6. **콘텐츠 15일치 생성** — 검수 통과 채널만 렌더/합성, `schedule-daily`로 배치 → 🚦 내용 체크.
7. **자동화 ON + 텔레그램** — `PSLAB_DRY_RUN=false` + 텔레그램 연결 → 🚦 첫 발행·수신 확인.

각 단계 시작 시 몇 단계인지 한 줄로 알리고, 끝나면 산출물 링크 + 다음 예고 + 게이트 질문으로 마무리한다.
**7단계 전체의 상세(산출물·명령어·게이트 기준)는 [docs/00-개요와-온보딩.md](docs/00-개요와-온보딩.md).**

---

## 문서 지도 (개념 → 상세 문서)

실제 지침은 아래 개념별 `docs/` 문서에 **단일 출처**로 나뉘어 있다. 작업할 개념의 문서를 열고,
"왜"가 궁금하면 각 문서 끝의 `../adr/` 링크로 간다.

| 개념 | 문서 | 무엇이 있나 |
|---|---|---|
| 개요·온보딩 | [docs/00-개요와-온보딩.md](docs/00-개요와-온보딩.md) | 아키텍처(무엇인가), 표준 진행 7단계 상세, 신규 업체 온보딩, 빠른 시작 체크리스트 |
| 도메인·데이터 | [docs/01-도메인-데이터.md](docs/01-도메인-데이터.md) | `clients/<id>.json`(ClientConfig)·`data/clients/<id>/*` 계약, 멀티클라이언트 격리, 시드 강제 커밋 규칙 |
| 콘텐츠 엔진 | [docs/02-콘텐츠-엔진.md](docs/02-콘텐츠-엔진.md) | AI 글·한글 이미지 렌더·사진·블로그/쇼츠·인스타 이미지 전면형·마감 장표·감도 학습·가짜글자·원고 규칙·렌더 세이프존 |
| 기획 지능 | [docs/03-기획-지능.md](docs/03-기획-지능.md) | 네이버 키워드 기반 기획(3시트·차주 스케줄링), 지침 시스템 v3, 자체 학습 선순환 |
| 채널 연동 | [docs/04-채널연동.md](docs/04-채널연동.md) | 자동/수동 채널 표, 채널별 토큰 발급, 비개발자 실전 플레이북 |
| 발행·스케줄링 | [docs/05-발행-스케줄링.md](docs/05-발행-스케줄링.md) | `publish-plan --due`, 일일 18시 배치, DRY_RUN·멱등, 무인 안전장치 3종 |
| 대시보드 | [docs/06-대시보드.md](docs/06-대시보드.md) | 구성·v2(라이트/기간/수정요청), 지침 탭, 레이아웃 원칙, 채널 스코프, 안정성 수칙 |
| 인프라·배포 | [docs/07-인프라-배포.md](docs/07-인프라-배포.md) | 워크플로 목록, Pages/배포 인프라 함정, 비용 |
| 함정·결정 | [docs/08-함정과-결정.md](docs/08-함정과-결정.md) | 하드-원 함정 요약 목록 → 각 개념 문서·`../adr/`로 링크 |
| 결정 기록(왜) | [adr/README.md](adr/README.md) | ADR-0000~0010 인덱스·기억 갱신 루프 |

---

## 결정 기록(ADR) · 기억 시스템 (요약)

이 스킬은 **docs/(어떻게=지침) + [`adr/`](adr/README.md)(왜=결정 이력)** 2계층으로 스스로 기억·갱신한다.
작업 시작 시 `adr/README.md` 인덱스를 읽어 관련 ADR을 확인하고, **되돌리기 어려운 결정은 다시 논쟁하지
않는다**. 중요한 결정·반복 함정을 만나면 `adr/template.md`로 **새 ADR을 추가**하고, 달라진 절차는 해당
`docs/` 문서에 반영하며, 코드·문서·ADR을 **한 커밋**으로 묶는다. 상세 규약·기준·인덱스는 [adr/README.md](adr/README.md).
