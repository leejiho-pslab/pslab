# 구글 블로그(Blogger) 자동발행 연동 가이드

네이버 블로그는 공식 자동발행 API가 없어 수동(복붙)이지만,
**구글 블로그(Blogger)는 공식 API로 자동발행**이 됩니다. 한 번만 아래를 설정하면
이후 cron이 예정 시각에 자동으로 글을 올립니다.

준비물 4개(시크릿): `클라이언트 ID`, `클라이언트 시크릿`, `리프레시 토큰`, `블로그 ID`

---

## 1단계 — 구글 클라우드에서 Blogger API 켜기
1. https://console.cloud.google.com → 상단에서 **새 프로젝트** 생성(이름 아무거나)
2. 검색창에 **"Blogger API"** → **사용 설정(Enable)**

## 2단계 — OAuth 동의 화면
1. 좌측 **API 및 서비스 → OAuth 동의 화면**
2. User Type **외부(External)** 선택 → 앱 이름/이메일만 입력해 저장
3. **테스트 사용자(Test users)** 에 **본인 구글 계정** 추가
4. ⚠️ **중요**: 동의 화면의 **게시 상태(Publishing status)** 를 **"앱 게시 / 프로덕션으로 푸시(Publish app)"** 로 바꾸세요.
   - 테스트 모드로 두면 **리프레시 토큰이 7일 만에 만료**되어 자동발행이 멈춥니다.
   - 게시하면 "확인되지 않은 앱" 경고가 뜨지만, 본인만 쓰는 앱이라 그대로 진행하면 토큰이 만료되지 않습니다.

## 3단계 — OAuth 클라이언트 ID 만들기 (ID/시크릿)
1. **API 및 서비스 → 사용자 인증 정보 → 사용자 인증 정보 만들기 → OAuth 클라이언트 ID**
2. 애플리케이션 유형 **웹 애플리케이션**
3. **승인된 리디렉션 URI** 에 추가: `https://developers.google.com/oauthplayground`
4. 만들면 **클라이언트 ID** 와 **클라이언트 보안 비밀(시크릿)** 이 나옵니다 → 복사

## 4단계 — 리프레시 토큰 받기 (OAuth Playground)
1. https://developers.google.com/oauthplayground 접속
2. 우측 상단 **⚙️(설정)** → **Use your own OAuth credentials** 체크 →
   3단계의 **클라이언트 ID/시크릿** 입력
3. 왼쪽 **Step 1** 입력칸에 스코프 직접 입력:
   `https://www.googleapis.com/auth/blogger`
   → **Authorize APIs** → 본인 구글 계정으로 로그인·허용
4. **Step 2** 에서 **Exchange authorization code for tokens** 클릭
5. 나오는 **Refresh token** (`1//...` 로 시작) 복사

## 5단계 — 블로그 ID 확인
- Blogger(https://www.blogger.com) 로그인 → 블로그 선택 → 주소창 URL에
  `blogID=숫자` 부분의 **숫자**가 블로그 ID 입니다.
  (예: `...blogger.com/blog/posts/**1234567890123456789**`)

## 6단계 — GitHub 시크릿 등록
👉 https://github.com/leejiho-pslab/pslab/settings/secrets/actions

| Name | 값 |
|---|---|
| `PSLAB_BLOGGER_CLIENT_ID` | 3단계 클라이언트 ID |
| `PSLAB_BLOGGER_CLIENT_SECRET` | 3단계 클라이언트 시크릿 |
| `PSLAB_BLOGGER_REFRESH_TOKEN` | 4단계 리프레시 토큰 (`1//...`) |
| `PSLAB_BLOGGER_BLOG_ID` | 5단계 블로그 ID(숫자) |

등록 후 **"넣었어"** 라고 하시면 제가 테스트 발행으로 실제로 글이 올라가는지 확인해 드립니다.

---

## 운영
- 블로거 발행 항목은 **네이버 블로그와 같은 완성글**을 사용합니다(네이버 다음 날 09:00 예정).
- 네이버는 그대로 **복붙 수동 발행**, 블로거는 **API 자동 발행** — 같은 글을 두 곳에 운영합니다.
- 리프레시 토큰은 만료되지 않습니다(앱을 삭제·재인증하지 않는 한). 인스타·스레드 토큰처럼 60일마다 갱신할 필요가 없습니다.
