PyYAML>=6.0
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pytest>=8.0
# 운영 DB(Postgres) 사용 시. SQLite(기본)만 쓰면 불필요.
psycopg[binary]>=3.1
# GA4(방문자수) 연동 시 서비스계정 토큰 발급용. 미연동이면 코드에서 사용되지 않음.
google-auth>=2.20
requests>=2.31
# 월간 리포트 Word(.docx) 다운로드 생성용.
python-docx>=1.1
