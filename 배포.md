# 24시간 무인 가동 배포 가이드

> 설계도의 마지막 한 조각 — **"항상 켜진 곳에서 데몬을 돌린다."**
> 데몬 코드는 다 만들어 뒀습니다(`pslab daemon`). 이제 **어디서 24시간 켜둘지**만
> 정하면 됩니다. 그 결정은 회원님 계정/결제가 필요해 제가 대신 못 합니다.

---

## 비유로 먼저

지금까지 만든 건 **"알아서 도는 공장 기계"** 입니다. 그런데 기계는 **전원이 켜진 공장 안**에
있어야 돌아가죠. 내 노트북은 껐다 켜고 잠들기 때문에 공장이 될 수 없어요.
그래서 **24시간 안 꺼지는 작은 임대 공간(서버)** 이 필요합니다.

---

## 어디서 돌릴까 — 3가지 선택지

| 방법 | 난이도 | 월 비용(대략) | 추천 대상 |
|------|--------|--------------|-----------|
| **A. 클라우드 작은 서버** (VPS) | 보통 | 5,000~10,000원 | 가장 무난 (추천) |
| **B. 관리형 호스팅** (Railway/Render 등) | 쉬움 | 무료~만원 | 서버 다루기 싫을 때 |
| **C. 집에 있는 미니PC/라즈베리파이** | 보통 | 전기값만 | 이미 장비가 있을 때 |

> **추천: A 또는 B.** 어느 쪽이든 제가 배포 설정을 끝까지 도와드립니다.
> 회원님은 **"여기서 돌리자"는 결정과 계정 가입**만 해주시면 됩니다.

---

## 가장 쉬운 길 — Docker 한 줄 (이미 준비됨)

어떤 서버를 고르든, 그 서버에서 아래만 하면 가동됩니다. (필요한 파일은 다 만들어 뒀어요)

```bash
# 1) 코드 받기
git clone <이 저장소 주소> && cd pslab

# 2) 자격 증명 입력 (.env.example 보고 .env 작성)
cp .env.example .env   # 그리고 키 채우기

# 3) 가동 (백그라운드로 24시간)
docker compose up -d
```

끝입니다. 이제 서버가 켜져 있는 한 **11시·19시에 알아서 발행**합니다.

확인/운영 명령:
```bash
docker compose logs -f                         # 돌아가는 로그 실시간 보기
docker compose exec pslab node dist/cli.js board   # 상황판
docker compose down                            # 정지
```

> `restart: unless-stopped` 라서 **서버가 재부팅돼도 자동으로 다시 켜집니다.**
> `TZ=Asia/Seoul` 로 **한국 시간** 기준 발행입니다.

---

## Docker 없이 돌리는 법 (대안)

```bash
npm install
npm run build
TZ=Asia/Seoul node dist/cli.js daemon
```

이 경우엔 프로세스가 안 꺼지게 `pm2` 같은 도구로 관리하는 걸 권장합니다:
```bash
npm i -g pm2
pm2 start "node dist/cli.js daemon" --name pslab --time
pm2 save && pm2 startup   # 재부팅 시 자동 시작
```

---

## 마운트(폴더 연결) 설명

- **`clients/`** — 클라이언트 설정표(JSON)들. 여기에 회사 추가 = 라인 추가.
- **`data/`** — 사이클 이력(격리 저장소). 지우면 강화 학습 기록이 사라지니 보존.

docker-compose가 이 둘을 서버 폴더와 연결해 둬서, **컨테이너를 새로 띄워도 데이터가 유지**됩니다.

---

## 회원님이 결정/준비할 것 (체크리스트)

- [ ] **어디서 돌릴지** (A 클라우드 서버 / B 관리형 / C 집 장비)
- [ ] 그 서비스 **계정 가입 + 결제수단** (회원님 명의)
- [ ] (실제 발행하려면) **SNS API 키**를 `.env`에 입력
- [ ] (실제 AI 글 생성하려면) **ANTHROPIC_API_KEY** 입력

> 위 중 **"어디서 돌릴지"** 만 정해 주시면, 그 환경에 맞춘 배포 절차를
> 제가 단계별로 더 자세히 만들어 드리겠습니다.
