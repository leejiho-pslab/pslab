# 유튜브(YouTube Data API v3) 자동발행 연동 가이드

인스타·스레드·블로거처럼 API로 무인 자동발행됩니다. 다만 유튜브는 **업로드 권한(youtube.upload)이
구글의 "제한된 범위(Restricted scope)"** 라서 블로거보다 한 단계 더 신경 쓸 게 있습니다 —
아래 ①/② 중 하나를 선택하세요.

준비물 4개(시크릿): `클라이언트 ID`, `클라이언트 시크릿`, `리프레시 토큰`, `채널 ID`

---

## 먼저 결정할 것 — 어느 방식으로 운영할지

| | ① 빠른 방식 (지금 바로) | ② 영구 방식 (검수) |
|---|---|---|
| 걸리는 시간 | 10분 | 검수 승인까지 며칠~몇 주 |
| 리프레시 토큰 | **7일마다 만료** → 그때마다 재발급 필요 | 만료 없음 |
| 지금 할 일 | 아래 1~6단계 | 아래 1~6단계 + 구글 앱 검수 신청 |
| 추천 대상 | 우선 다시 돌리고 싶을 때 | 완전 무인을 원할 때 |

**추천: 우선 ①로 오늘 다시 돌리고, 검수(②)는 여유될 때 따로 신청.**
검수 없이 "앱 게시"만 눌러도 되는 블로거와 달리, 유튜브 업로드 권한은 검수를 통과하지 못하면
**영상이 자동으로 "비공개"로만 올라가는 제약**이 있어 완전한 해결책은 아닙니다(아래 8단계 참고).

---

## 1단계 — 구글 클라우드에서 YouTube Data API 켜기
1. https://console.cloud.google.com → 기존 프로젝트가 있으면 선택(블로거 연동 때 만든 프로젝트 재사용 가능), 없으면 **새 프로젝트** 생성
2. 검색창에 **"YouTube Data API v3"** → **사용 설정(Enable)**

## 2단계 — OAuth 동의 화면
1. 좌측 **API 및 서비스 → OAuth 동의 화면**
2. User Type **외부(External)** 선택 → 앱 이름/이메일만 입력해 저장
3. **테스트 사용자(Test users)** 에 **유튜브 채널 소유 구글 계정** 추가
4. 게시 상태는 일단 **"테스트(Testing)"** 로 둡니다 (지금은 ① 빠른 방식 기준 — ② 검수는 8단계에서 별도 진행)

## 3단계 — OAuth 클라이언트 ID 만들기 (ID/시크릿)
1. **API 및 서비스 → 사용자 인증 정보 → 사용자 인증 정보 만들기 → OAuth 클라이언트 ID**
2. 애플리케이션 유형 **웹 애플리케이션**
3. **승인된 리디렉션 URI** 에 추가: `https://developers.google.com/oauthplayground`
4. 만들면 **클라이언트 ID** 와 **클라이언트 보안 비밀(시크릿)** 이 나옵니다 → 복사
   (블로거 때 만든 클라이언트를 재사용해도 됩니다 — 새로 안 만들어도 OK)

## 4단계 — 리프레시 토큰 받기 (OAuth Playground)
1. https://developers.google.com/oauthplayground 접속
2. 우측 상단 **⚙️(설정)** → **Use your own OAuth credentials** 체크 →
   3단계의 **클라이언트 ID/시크릿** 입력
3. 왼쪽 **Step 1** 입력칸에 스코프 두 개를 **줄바꿈으로 구분**해 직접 입력:
   ```
   https://www.googleapis.com/auth/youtube.upload
   https://www.googleapis.com/auth/youtube.readonly
   ```
   → **Authorize APIs** → **영상을 올릴 그 유튜브 채널의 구글 계정**으로 로그인·허용
   ("확인되지 않은 앱" 경고가 뜨면 **고급(Advanced) → (앱 이름)으로 이동(안전하지 않음)** 클릭 — 본인 앱이라 안전합니다)
4. **Step 2** 에서 **Exchange authorization code for tokens** 클릭
5. 나오는 **Refresh token** (`1//...` 로 시작) 복사

## 5단계 — 채널 ID 확인
- https://www.youtube.com/account_advanced 접속(해당 계정으로 로그인 상태) →
  **채널 ID** 칸의 문자열(`UC`로 시작) 복사

## 6단계 — GitHub 시크릿 등록
👉 https://github.com/leejiho-pslab/pslab/settings/secrets/actions

| Name | 값 |
|---|---|
| `PSLAB_YOUTUBE_CLIENT_ID` | 3단계 클라이언트 ID |
| `PSLAB_YOUTUBE_CLIENT_SECRET` | 3단계 클라이언트 시크릿 |
| `PSLAB_YOUTUBE_REFRESH_TOKEN` | 4단계 리프레시 토큰 (`1//...`) |
| `PSLAB_YOUTUBE_CHANNEL_ID` | 5단계 채널 ID (`UC...`) |

등록 후 **"넣었어"** 라고 하시면 제가 토큰 점검 + 테스트 발행으로 실제로 영상이 올라가는지 확인해 드립니다.

---

## 7단계 — 7일마다 반복해야 하는 일 (① 빠른 방식 선택 시)
테스트 상태인 동안은 **리프레시 토큰이 7일마다 만료**됩니다. 대시보드에 만료 경고가 뜨면
(또는 제가 "유튜브 토큰 만료" 라고 알려드리면) **4단계만 다시** 반복해 새 리프레시 토큰을
`PSLAB_YOUTUBE_REFRESH_TOKEN` 시크릿에 덮어쓰면 됩니다(1~3, 5단계는 그대로 재사용).
원하시면 매주 알림을 보내드리는 루틴을 만들어 놓겠습니다 — 말씀만 주세요.

## 8단계 — 영구 해결(검수) 원하실 때
검수(Compliance Audit)를 통과하면 리프레시 토큰이 만료되지 않고, "비공개 업로드만 가능"
제약도 풀려 완전 자동 공개 발행이 됩니다. 필요한 것:
- 개인정보처리방침 페이지 URL (pslab.ai.kr에 간단히 한 페이지 추가하면 됩니다 — 제가 만들어 드릴 수 있어요)
- 앱 사용 목적 설명 + OAuth 동의 흐름을 보여주는 데모 영상
- 유튜브 API 서비스 약관 동의

신청 절차는 제가 문서·데모 영상 스크립트까지 도와드릴 수 있습니다. 진행 원하시면 말씀해 주세요.

---

## 운영
- 지금 발행 대기 중인 유튜브 항목 3건(7/27·7/29·7/31)은 시크릿 등록 즉시 다음 사이클에 자동으로 나갑니다.
- 리프레시 토큰이 만료되면 대시보드 상단에 경고가 뜨고, 그 사이클에서는 유튜브만 건너뛰고 나머지 채널은 정상 발행됩니다(전체가 멈추지 않습니다).
